# Optional direnv integration. direnv is NOT installed in this environment, so this
# file is inert unless you install direnv (https://direnv.net) and run `direnv allow`.
# Without direnv, enter the environment manually with:  nix develop
use flake
